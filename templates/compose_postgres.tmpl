version: "3.8"

services:
    postgres:
        environment:
            - POSTGRES_USER=postgres
            - POSTGRES_PASSWORD=postgres
            - POSTGRES_DB=test
        image: postgres:latest
        container_name: postgres_db
        logging:
            options:
                max-file: "3"
                max-size: 10m
        ports:
            - 5433:5432
        restart: always
        networks:
            - postgres_net

networks:
    postgres_net:
        driver: bridge